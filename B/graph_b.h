#ifndef PROTOTYPE
#define PROTOTYPE = 1
using namespace std;
template <typename T>
class Graph;
// template <typename T>
// auto Tarjans_algorithm (Graph<T> graph);
// template <typename T>
// void dfs_inv(vector<size_t> &stack, vector<bool> &visited, size_t vertex, Graph<T> graph);

// template <typename T>
// bool find_in_stack(T stack, int vertex);


#endif